# Debugging Rule (Global)

Universal invariant:

1. Start debugging from machine-readable runtime artifacts first (`ai/symbols-*.json`, stack trace target files, `openapi.json` for API behavior).
2. Use detailed methodology from `.claude/context/key-context/debugging-patterns.md`.
3. Use symbol query recipes from `.claude/context/key-context/symbols-query-playbook.md`.
