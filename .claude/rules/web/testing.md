# Testing Rule (Pointer)

Path scope: `skillmeat/web/__tests__/**`, `skillmeat/web/tests/**`

Use `.claude/context/key-context/testing-patterns.md` for full test patterns.

Invariant:
- Prefer accessible queries (`getByRole`, `getByLabelText`) over test IDs.
