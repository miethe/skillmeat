# App Router Rule (Pointer)

Path scope: `skillmeat/web/app/**/*.tsx`

Use `.claude/context/key-context/nextjs-patterns.md` for full App Router conventions.

Invariants:
- Default to server components.
- Add `'use client'` only when client runtime features are required.
