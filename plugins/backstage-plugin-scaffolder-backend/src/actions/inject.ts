/**
 * skillmeat:context:inject - Backstage scaffolder action
 *
 * Calls POST /api/v1/integrations/idp/scaffold on the SAM API,
 * then writes the returned files into the Backstage scaffolder workspace.
 *
 * Implementation in TASK-2.2.
 */
export {};
